import 'package:flutter/material.dart';

class BGBoxShadow {
  static BoxShadow shadowSig = BoxShadow(
      offset: Offset(0, 12),
      blurRadius: 24,
      spreadRadius: -8,
      color: Color(0x8cff4a68));
}
